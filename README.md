# 学校コミュニケーションアプリ

生徒・保護者・職員のための学校コミュニケーションプラットフォーム

## 📱 プロジェクト概要

このアプリケーションは、学校内のコミュニケーションを円滑にするために開発された、生徒約30名、職員約10名規模の学校向けプラットフォームです。

### 主な特徴

- **完全無料**: Firebase無料枠とVercel無料枠で運用可能
- **技術者不要**: シンプルな管理画面で誰でも運用可能
- **リアルタイム更新**: 投稿やメッセージが即座に反映
- **レスポンシブデザイン**: スマホ・タブレット・PCに対応

## 🎯 含まれる機能

### 📱 生徒・保護者向けアプリ (`index.html`)

1. **ホーム画面**
   - 最新のお知らせ表示
   - 直近のイベント表示

2. **掲示板**
   - 投稿の閲覧（付箋スタイル）
   - カテゴリ別表示

3. **カレンダー**
   - イベント一覧表示
   - 日時詳細表示

4. **メッセージ**
   - 職員からのメッセージ受信
   - 既読/未読管理

### 💼 管理者向けツール (`admin.html`)

1. **ダッシュボード**
   - ユーザー数、投稿数、メッセージ数の統計
   - 最近のアクティビティ表示

2. **投稿管理**
   - 投稿の作成・編集・削除
   - リアルタイムプレビュー

3. **メッセージ管理**
   - 個別メッセージの送信
   - 送信済みメッセージの閲覧・削除
   - 既読/未読状態の確認

4. **ユーザー管理**
   - ユーザーの追加・削除
   - 権限設定（管理者/職員/生徒）
   - クラス情報の管理

5. **イベント管理**
   - イベントの登録・削除
   - 日時・詳細情報の設定

## 🛠️ 技術スタック

- **フロントエンド**: HTML5, Tailwind CSS, JavaScript (ES6+)
- **バックエンド**: Firebase (Authentication, Firestore, Storage)
- **ホスティング**: Vercel (推奨)
- **アイコン**: Font Awesome 6

## 📦 Firebase データ構造

### `users` コレクション
```javascript
{
  uid: string,          // Firebase Authentication UID
  name: string,         // ユーザー名
  email: string,        // メールアドレス
  role: string,         // 'admin' | 'staff' | 'student'
  class: string,        // クラス情報（任意）
  createdAt: timestamp  // 作成日時
}
```

### `posts` コレクション
```javascript
{
  title: string,        // 投稿タイトル
  content: string,      // 投稿内容
  authorId: string,     // 投稿者UID
  authorName: string,   // 投稿者名
  createdAt: timestamp, // 作成日時
  updatedAt: timestamp  // 更新日時
}
```

### `messages` コレクション
```javascript
{
  senderId: string,     // 送信者UID
  senderName: string,   // 送信者名
  recipientId: string,  // 受信者UID
  recipientName: string,// 受信者名
  subject: string,      // 件名
  content: string,      // 内容
  isRead: boolean,      // 既読フラグ
  createdAt: timestamp  // 送信日時
}
```

### `events` コレクション
```javascript
{
  title: string,        // イベント名
  description: string,  // イベント説明
  eventDate: timestamp, // イベント日時
  createdAt: timestamp  // 作成日時
}
```

## 🚀 セットアップ手順

### 1. Firebase プロジェクトの作成

1. [Firebase Console](https://console.firebase.google.com/) にアクセス
2. 「プロジェクトを追加」をクリック
3. プロジェクト名を入力（例: school-communication-app）
4. Google アナリティクスは任意で有効化
5. プロジェクトを作成

### 2. Firebase Authentication の設定

1. Firebase Console で「Authentication」を選択
2. 「始める」をクリック
3. 「Sign-in method」タブを選択
4. 「メール/パスワード」を有効化

### 3. Firestore Database の設定

1. Firebase Console で「Firestore Database」を選択
2. 「データベースの作成」をクリック
3. **本番モード**で開始（セキュリティルールは後で設定）
4. ロケーションを選択（推奨: asia-northeast1（東京））

#### Firestore セキュリティルールの設定

Firestore Database の「ルール」タブで以下を設定：

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ユーザーコレクション
    match /users/{userId} {
      allow read: if request.auth != null;
      allow create: if request.auth != null;
      allow update, delete: if request.auth != null && 
        (get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin' ||
         request.auth.uid == userId);
    }
    
    // 投稿コレクション
    match /posts/{postId} {
      allow read: if request.auth != null;
      allow create: if request.auth != null && 
        (get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin' ||
         get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'staff');
      allow update, delete: if request.auth != null && 
        (get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin' ||
         get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'staff');
    }
    
    // メッセージコレクション
    match /messages/{messageId} {
      allow read: if request.auth != null && 
        (resource.data.recipientId == request.auth.uid ||
         resource.data.senderId == request.auth.uid ||
         get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin');
      allow create: if request.auth != null && 
        (get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin' ||
         get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'staff');
      allow update, delete: if request.auth != null && 
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }
    
    // イベントコレクション
    match /events/{eventId} {
      allow read: if request.auth != null;
      allow create: if request.auth != null && 
        (get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin' ||
         get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'staff');
      allow update, delete: if request.auth != null && 
        (get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin' ||
         get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'staff');
    }
  }
}
```

### 4. Firebase Storage の設定（オプション）

将来的に画像アップロード機能を追加する場合：

1. Firebase Console で「Storage」を選択
2. 「始める」をクリック
3. デフォルト設定で開始

### 5. Firebase 設定情報の取得

1. Firebase Console のプロジェクト概要で「ウェブアプリを追加」をクリック
2. アプリのニックネームを入力
3. 表示される設定情報をコピー

### 6. アプリケーションの設定

`index.html` と `admin.html` の両方で、以下の部分を書き換えます：

```javascript
const firebaseConfig = {
    apiKey: "YOUR_API_KEY",
    authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
    projectId: "YOUR_PROJECT_ID",
    storageBucket: "YOUR_PROJECT_ID.appspot.com",
    messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
    appId: "YOUR_APP_ID"
};
```

実際の値に置き換えてください。

### 7. 初期管理者ユーザーの作成

Firebase Console の Authentication タブで手動でユーザーを作成：

1. 「ユーザー」タブを選択
2. 「ユーザーを追加」をクリック
3. メールアドレスとパスワードを入力
4. ユーザーを作成

次に、Firestore Database で手動でユーザー情報を追加：

1. Firestore Database の「データ」タブを選択
2. 「コレクションを開始」をクリック
3. コレクションID: `users`
4. 「ドキュメントID」に上記で作成したユーザーのUIDを入力
5. フィールドを追加：
   - `uid` (string): Firebase AuthenticationのUID
   - `name` (string): 管理者名
   - `email` (string): メールアドレス
   - `role` (string): `admin`
   - `createdAt` (timestamp): 現在時刻

### 8. Vercel へのデプロイ

1. [Vercel](https://vercel.com/) にアクセスしてサインアップ
2. 「New Project」をクリック
3. GitHubリポジトリを接続するか、ファイルを直接アップロード
4. デプロイ設定はデフォルトのまま
5. 「Deploy」をクリック

デプロイ後、Vercel が提供するURLにアクセスすれば利用可能です。

## 🔗 アクセスURL

デプロイ後のURL例：

- **生徒・保護者向けアプリ**: `https://your-project.vercel.app/`
- **管理者ツール**: `https://your-project.vercel.app/admin.html`

## 👥 ユーザー管理の流れ

### 新規ユーザーの登録手順

1. 管理者ツール (`admin.html`) にログイン
2. 「ユーザー管理」をクリック
3. 「新規ユーザー」をクリック
4. 必要情報を入力：
   - 名前
   - メールアドレス
   - パスワード（初期パスワード）
   - 権限（生徒/職員/管理者）
   - クラス（任意）
5. 「登録」をクリック
6. 登録したメールアドレスとパスワードをユーザーに伝える

### ユーザーの削除手順

1. 管理者ツールの「ユーザー管理」にアクセス
2. 削除したいユーザーの行の「ゴミ箱」アイコンをクリック
3. 確認ダイアログで「OK」をクリック

**注意**: Firestoreのデータのみ削除され、Firebase Authenticationのアカウントは残ります。

## 📝 運用ガイド

### 投稿の作成

1. 管理者ツールにログイン
2. 「投稿管理」をクリック
3. 「新規投稿」をクリック
4. タイトルと内容を入力
5. 「投稿する」をクリック

### メッセージの送信

1. 管理者ツールにログイン
2. 「メッセージ管理」をクリック
3. 「新規メッセージ」をクリック
4. 受信者を選択
5. 件名と内容を入力
6. 「送信」をクリック

### イベントの登録

1. 管理者ツールにログイン
2. 「イベント管理」をクリック
3. 「新規イベント」をクリック
4. イベント名、説明、日時を入力
5. 「登録」をクリック

## 🔒 セキュリティ

- Firebase Authenticationによる認証
- Firestoreセキュリティルールによる権限管理
- 管理者・職員・生徒の3段階の権限設定
- すべての操作はログイン必須

## 💰 コスト

### Firebase 無料枠（Sparkプラン）

- **Authentication**: 無制限（メール/パスワード認証）
- **Firestore**: 
  - ストレージ: 1GB
  - 読み取り: 50,000回/日
  - 書き込み: 20,000回/日
  - 削除: 20,000回/日
- **Storage**: 5GB（今回は未使用）

**40名規模での試算**: 完全無料枠内で運用可能

### Vercel 無料枠（Hobby）

- **帯域幅**: 100GB/月
- **ビルド時間**: 100時間/月
- **デプロイ**: 無制限

**40名規模での試算**: 完全無料枠内で運用可能

## 🐛 トラブルシューティング

### ログインできない

1. Firebase設定が正しく入力されているか確認
2. Firebase Consoleで該当ユーザーが存在するか確認
3. Firestoreに対応するユーザードキュメントが存在するか確認

### データが表示されない

1. Firestoreセキュリティルールが正しく設定されているか確認
2. ブラウザのコンソール（F12）でエラーを確認
3. Firebase Consoleでデータが存在するか確認

### 管理者ツールにアクセスできない

1. ログインユーザーの `role` が `admin` または `staff` になっているか確認
2. Firestoreのusersコレクションで権限を確認

## 🔄 今後の拡張可能性

- [ ] 画像アップロード機能
- [ ] プッシュ通知
- [ ] グループメッセージ
- [ ] ファイル添付機能
- [ ] 既読確認機能の強化
- [ ] カレンダーのUI改善
- [ ] ダークモード対応
- [ ] PWA対応（オフライン機能）

## 📞 サポート

問題が発生した場合は、以下を確認してください：

1. Firebase Consoleでエラーログを確認
2. ブラウザの開発者ツール（F12）でコンソールエラーを確認
3. Firestoreセキュリティルールが正しく設定されているか確認

## 📄 ライセンス

このプロジェクトは教育目的で作成されています。

---

**最終更新**: 2025年12月17日
**対象規模**: 生徒30名、職員10名
**推奨環境**: Chrome, Safari, Edge最新版